const nombre = 'William';
const apellido = 'Ricalde';

const nombreCompleto = `${nombre} ${apellido}`;

console.log( nombreCompleto );

function getSaludo () {
    return 'Hola mundo'
}

console.log(`Este es un texto ${getSaludo()}`)